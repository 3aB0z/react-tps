import Layout from "./Layout";
// import { useState } from "react";
// import { useSelector } from "react-redux";

function App() {
  // const [data, setData] = useState([]);
  // const oldData = useSelector((state)=>state.);

  return (
    <>
      <Layout />
    </>
  );
}

export default App;
